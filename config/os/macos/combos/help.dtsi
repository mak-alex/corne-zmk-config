combo_mac_help-mBAS {
    bindings = <&kp LA(F_mBAS)>;
    key-positions = <15 20>;
    slow-release;
    layers = <mBAS>;
};

combo_mac_help-mDEV {
    bindings = <&kp LA(F_mDEV)>;
    key-positions = <15 20>;
    slow-release;
    layers = <mDEV>;
};

combo_mac_help-mAXN {
    bindings = <&kp LA(F_mAXN)>;
    key-positions = <15 20>;
    slow-release;
    layers = <mAXN>;
};

combo_mac_help-mFNK {
    bindings = <&kp LA(F_mFNK)>;
    key-positions = <15 20>;
    slow-release;
    layers = <mFNK>;
};

combo_mac_help-mSTG {
    bindings = <&kp LA(F_mSTG)>;
    key-positions = <15 20>;
    slow-release;
    layers = <mSTG>;
};